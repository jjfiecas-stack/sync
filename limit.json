{
  "metadata": {
    "name": "globalResultLimit",
    "author": "jjfiecas-stack",
    "description": "Global Result Limit",
    "version": "1.0.0"
  },
  "value": "/*Global Result Limit: 6*/slice(negate(merge(library(streams), cached(seadex(streams))), streams), 6)"
}
